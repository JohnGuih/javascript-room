<h1>Lista de posts: </h1>

{{#each posts}}

    <small>{{dataValues.createdAt}}</small>
    <h1>{{dataValues.titulo}}</h1>
    <p>{{dataValues.conteudo}}</p>
    <form action="/deletar/{{dataValues.id}}">
    <input type="submit" value="Deletar" />
    </form>
    <hr>

{{/each}}