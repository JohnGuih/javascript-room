<div class="bg-light p-4">
    <h1 class="display-4">Bem vindo ao Blog</h1>
    <p class="lead">This is a simple hero unit, a simple jumbotron-style component for calling extra at...</p>
    <hr class="my-4">
    <p>It uses utility classes for typography and space content out within the larger contai...</p>
    <p class="lead">
        <a class="btn btn-primary btn-lg" href="#" role="button">Crie uma conta</a>
    </p>
</div>

<hr>

<h2>Postagens recentes</h2>


{{#each postagens}}

<div class="card">
    <div class="card-body">
        <h3>{{titulo}}</h3>
        <p>{{descricao}}</p>
    </div>
</div>

{{else}}

    <h5>Nenhuma postagem</h5>

{{/each}}